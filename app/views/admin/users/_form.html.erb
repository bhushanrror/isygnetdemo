<%= semantic_form_for [:admin, @user] do |f| %>
   <%=f.inputs "User Details" do
      f.input :username    
    end %>
    <%=f.inputs "email" do     
      f.input :email , :label => "User Email"
    end %>
    <%= f.actions  %>
<% end %>